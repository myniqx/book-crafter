import React from 'react'
import { EntityBrowser } from '@renderer/components/entities/EntityBrowser'

export const EntityBrowserPanel: React.FC = () => {
  return <EntityBrowser className="h-full" />
}
